using System;
using System.Threading.Tasks;
namespace Task05
{
    class Program
    {
        static void Main()
        {
          /*  var fTree = new FineBST();
            var cTree = new CoarseBST();
            int[] vals = { 13, 12, 18, 19, 11, 17, 16, 10, 15 };
            bool[] has = new bool[9];
            
            Parallel.For(0, 9, i => fTree.Insert(vals[i]));
            Parallel.For(0, 9, i => has[i] = fTree.Find(vals[i]));
            for (int i = 0; i < 9; ++i)
            {
                Console.WriteLine($"tree has {vals[i]} -- {has[i]}");
            }
            Console.WriteLine($"FineBST is correct -- {fTree.IsCorrect()}\n");

            Parallel.For(0, 9, i => cTree.Insert(vals[i]));
            Parallel.For(0, 9, i => has[i] = cTree.Find(vals[i]));
            for (int i = 0; i < 9; ++i)
            {
                Console.WriteLine($"CoarseBST has {vals[i]} -- {has[i]}");
            }
            Console.WriteLine($"CoarseBST is correct -- {cTree.IsCorrect()}\n");
       */ }
    }
}
